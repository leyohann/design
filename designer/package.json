{
  "scripts": {
  "test": "echo \"Error: no test specified\" && exit 1",
  "compile": "sass input.scss style.css",
  "watch": "sass --watch input.scss style.css"
  },
  "devDependencies": {
    "sass": "^1.55.0"
  }
}
